{
  "name": "pegasus-loader",
  "version": "1.1.0",
  "description": "Project Structure based Route declarations for React Router and Webpack",
  "repository": {
    "type": "git",
    "url": "https://github.com/tazsingh/pegasus-loader.git"
  },
  "main": "dist/index.js",
  "jsnext/main": "src/index.js",
  "scripts": {
    "prepublish": "npm run clean && npm run build",
    "clean": "rimraf dist",
    "build": "babel src --out-dir dist"
  },
  "author": "Tasveer Singh",
  "license": "MIT",
  "engines": {
    "node": ">=4.0.0"
  },
  "devDependencies": {
    "babel-cli": "^6.6.5",
    "babel-core": "^6.7.4",
    "babel-preset-es2015": "^6.6.0",
    "rimraf": "^2.4.3"
  },
  "dependencies": {
    "loader-utils": "^0.2.12"
  }
}
