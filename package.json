{
  "name": "pegasus-loader",
  "version": "1.0.0",
  "description": "File-System based Route declarations for React Router",
  "repository": {
    "type": "git",
    "url": "https://github.com/tazsingh/pegasus-loader.git"
  },
  "main": "dist/index.js",
  "jsnext/main": "src/index.js",
  "scripts": {
    "prepublish": "npm run clean && npm run build",
    "clean": "rimraf dist",
    "build": "babel src --out-dir dist"
  },
  "author": "Tasveer Singh",
  "license": "MIT",
  "engines": {
    "node": ">=0.12.0"
  },
  "devDependencies": {
    "rimraf": "^2.4.3"
  },
  "dependencies": {
    "loader-utils": "^0.2.12"
  }
}
